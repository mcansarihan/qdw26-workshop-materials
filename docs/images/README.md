# README screenshots

Drop the following PNGs here (filenames are referenced by the top-level
`README.md`). Until they're added, the README shows the image's alt text.

| File | What it should show |
|---|---|
| `brev-recommended-config.png` | The Brev launchable deploy panel with the recommended config (16 CPU / 16 GiB RAM / GCP / 64 GiB). |
| `workspace-building.png` | The environment card while **Deploying / Container Waiting**. |
| `workspace-ready.png` | The environment card when **Running / Built** (with "Access Notebook"). |
| `workspace-access-page.png` | The environment's Access page (Logs / Content / Access tabs). |
| `secure-links.png` | The "Using Secure Links" table (ports 8888 / 8080 / 6080). |
| `novnc-landing.png` | The noVNC landing "Directory listing" page (click `vnc.html`). |
| `vnc-right-click-terminal.png` | The web desktop: welcome terminal + the right-click app menu (Terminal / KLayout / ParaView). |
| `vnc-apps-opened.png` | The web desktop with **KLayout and ParaView** open. |
| `ide-open-settings.png` | Cursor/VS Code Command Palette → Open User Settings (JSON). |
| `ide-attach-container.png` | Command Palette → "Attach to Running Container" → picking `…-dev-1`. |
| `ide-open-folder.png` | Opening `/home/ubuntu/qdw-workshop-materials` inside the attached window. |
